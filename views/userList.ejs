<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
		<title>User List</title>
		<link rel="stylesheet" type="text/css" href="/stylesheets/style.css">
	</head>
	<body>
		<div class="board-page">
			<div class="board">
				
				<h1>User List</h1>

                <!-- 검색 시작 -->
                <form action="/users/search/process" method="post">
                    <div class="search" align="right">
                        <input id="key" name="key" type="text" placeholder="<%= key%>"/>
                        <button id="btn_search" type="submit" value="send">검색</button>
                    </div>
        
                    <!-- 검색 종료 -->
                </form>
			
				<table class="title">
					<tr>
						<th width="20%">번호</th>
						<th width="40%">이름</th>
						<th width="20%">email</th>
						<th width="20%">등록일</th>
					</tr>
				</table>
				<table class="content">
					<% if(status == 'OK'){%>
                        <% if(data.length == 0){ %>
							<tr>
								<td colspan="4">NO DATA</td>
							</tr>
							<% } else { %>
						<% data.forEach(function(item, index){ %>
							<tr>
								<!--get 방식으로 bid 를 넘겨줌 ... 내용보기 가능-->
								<td width="20%"><a href="/users/update?uid=<%=item.uid%>"><%= item.uid %></a></td>
								<td width="40%"><%= item.user_name %></td>
                                <td width="20%"><%= item.email %></td>
								<td width="20%"><%= item.cdate2 %></td>
							</tr>
						
					<%	}); %>
					<% } %>
					<% } else { %>
					<tr>
						<td colspan="4">ERROR .. 잠시 후 다시 시도해주세요.</td>
					</tr>
					<% } %>
				</table>
				<div align="right">
						<button id="btn_toBoard_list">Board</button>
				</div>
				
			</div>
		</div>
		<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
        <script src="/javascripts/users.js"></script>
	</body>

</html>